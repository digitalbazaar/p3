To: {{notify.email}}
From: "{{service.name}} {{system.name}}" <{{system.email}}>
Subject: {{subject.prefix}}{{service.name}} PaymentToken intervention required, verify FAILED for {{tokenId}}

Machine:  {{machine}}
Token ID: {{tokenId}}
